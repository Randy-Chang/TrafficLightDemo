# 專案協作規範

## 專案說明

- 本專案是 .NET Framework 4.8 WinForms 專案。
- 未經明確要求，不得變更目標框架或升級至其他 .NET 版本。

## Agent 注意事項

- 讀取 Markdown 或其他含中文的文字檔時，使用 `Get-Content -Encoding UTF8`，避免中文內容因預設編碼而損壞。
- 若 `apply_patch` 找不到預期文字，先以 UTF-8 重新讀取檔案，確認實際內容、BOM 與行尾格式。
- 修補失敗時，不得改用 Shell 指令批次重寫整個檔案以繞過內容不符問題。
- 架構文件與程式中的中文註解必須維持 UTF-8 編碼。
- 統一 CRLF 時，只處理本次碰過的文字檔，並使用不會改變原有 UTF-8 BOM 策略或破壞中文內容的方式。

## 程式碼探索

- 若環境已安裝且可使用 codebase-memory MCP，開始分析或修改前，先用它了解相關架構與程式碼關係，再提出結論或進行實作；若未安裝或無法使用，可略過此要求。
- 使用 codebase-memory MCP 時，程式碼探索依序優先使用 `search_graph`、`trace_path`、`get_code_snippet`、`query_graph`、`get_architecture`。
- 查找字串常值、錯誤訊息、設定值、非程式碼檔案，或 codebase-memory MCP 未安裝、無法使用、資訊不足時，改用 `rg`、檔案搜尋或其他文字搜尋工具。

## 文字檔與行尾

- 修改完成後，將本次碰過的所有文字檔統一為 Windows CRLF（`\r\n`）。
- 交付前逐一檢查本次碰過的文字檔，確認沒有混用 CRLF、LF 或 CR 行尾。
- 不要為了統一行尾而改寫本次工作範圍以外的檔案。

## WinForms UI

- UI 必須使用 WinForms Designer 標準模式建立，並可在 Visual Studio Designer 中視覺化檢視及編輯；不要將整個 UI 改成由執行階段程式碼動態產生。
- `.Designer.cs` 僅負責控制項的宣告、建立、階層、位置、尺寸、版面配置與初始視覺屬性，並保留在 `InitializeComponent` 中。
- 控制項的資料內容初始化不得放在 `.Designer.cs`，包括 `ComboBox`、`ListBox` 等控制項的 `Items`、`DataSource`、選項集合或大量靜態資料；這些內容應在對應的 `.cs` 中初始化或繫結。
- 不要在 `.Designer.cs` 中加入事件繫結或行為邏輯；事件繫結、事件處理、狀態協調與商業邏輯都放在對應的 `.cs` 檔案中。
- 控制項的事件繫結、執行期狀態、內容更新及資料填充均放在對應的 `.cs` 中。
- 執行期間對 UI 元素的新增、移除、內容更新、狀態切換或樣式修改，放在對應的 `.cs` 檔案中；例如 `Items.Add`、`Items.AddRange`、`Nodes.Add` 或設定 `DataSource`。
- 保持表單 partial class、`Dispose`、元件容器與資源檔的標準 Designer 結構，確保 Visual Studio Designer 能正常開啟及編輯。

## 架構與設計原則

- 修改時遵循現有方案、專案、命名空間、資料夾與相依關係的既有架構。
- 遵循 SOLID 原則，優先維持單一職責、低耦合、依賴抽象與介面職責分離。
- UI 僅負責呈現與使用者互動；可獨立測試或重用的狀態流程、控制邏輯與輸出行為應留在適當的服務、控制器或介面實作中。
- 擴充既有介面或抽象前，先確認責任邊界與實際需求；避免過度設計，也不要只為套用設計模式而增加不必要的抽象層。
- 優先採取符合現有設計、範圍最小且可清楚驗證的修改。

## 禁止事項

- 未經明確要求，不得變更主要架構或專案目錄結構。
- 未經明確要求，不得加入新套件或升級框架版本。
- 不得覆寫、回退或清除使用者已存在但與目前任務無關的修改。
